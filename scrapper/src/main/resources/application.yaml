app:
  github-token: "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" # env variable
  stackoverflow:
    key: ${rl_MyaULbxY6yP8JivKmP5U8nTrd}
    access-token: ${SO_ACCESS_TOKEN}

spring:
  application:
    name: Scrapper
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

server:
  port: 8081

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui
